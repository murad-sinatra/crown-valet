# Crown Valet — Claude Instructions

## Committing Code

Before making any `git commit`, always run the `/code-review` skill first.
Do not skip this step under any circumstances.

## Branching

Never push directly to `main`. Always create a feature branch, push to that branch, and open a pull request for the user to review and merge. Do not merge it yourself.
